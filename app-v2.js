const log=document.getElementById('log');let game=null;try{game=typeof WARLAND_BOOT==='function'?WARLAND_BOOT():null}catch(e){console.error('WARLAND_BOOT error',e)}if(!game)game={date:{year:1939,month:9,day:1},turn:0,paused:false,economy:{treasury:100},news:[],events:[]};game.economy=game.economy||{treasury:100};game.date=game.date||{year:1939,month:9,day:1};window.game=game;function fmtDate(d){return d?`${d.day} / ${d.month} / ${d.year}`:'۱ سپتامبر ۱۹۳۹'}function report(text){if(!log)return;log.insertAdjacentHTML('beforeend',`<p>${text}</p>`);log.scrollTop=log.scrollHeight}function syncUI(){const m=document.getElementById('money');if(m)m.textContent=`${game.economy?.treasury??100}M`;const d=document.getElementById('gameDate');if(d&&game.date)d.textContent=fmtDate(game.date)}window.addEventListener('warland-map-select',e=>{const d=e.detail||{};report(`📍 ${d.name||'موقعیت'} انتخاب شد · ${d.owner||'نقطه استراتژیک'}`)});const end=document.getElementById('endTurn');if(end)end.onclick=()=>{game.paused=false;if(typeof tickGame==='function')tickGame(game);game.economy=game.economy||{};game.economy.treasury=(game.economy.treasury??100)+5;if(typeof emitGameEvent==='function')emitGameEvent(game,{type:'economic',title:'گزارش اقتصادی روزانه',message:'درآمد روزانه کشور ثبت شد.',data:{treasury:game.economy.treasury}});syncUI();report(`📅 ${fmtDate(game.date)} · درآمد روزانه دریافت شد.`);if(typeof getLatestNews==='function'){const n=getLatestNews(game,1)?.[0];if(n)report(`📰 ${n.title}`)}};async function askJessi(question){report('🤖 JESSI در حال تحلیل وضعیت بازی...');try{const r=await fetch('/api/jessi',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question,state:game})});const data=await r.json();if(!r.ok)throw new Error(data.error||'خطای اتصال');report(`🤖 JESSI: ${data.text||'پاسخی دریافت نشد.'}`)}catch(e){report('⚠️ اتصال JESSI برقرار نشد؛ Backend بازی باید Deploy شود.')}}document.querySelectorAll('.jessi').forEach(j=>j.onclick=()=>{const q=prompt('از JESSI درباره وضعیت کشور، اقتصاد، دیپلماسی، ارتش یا جنگ بپرس:','وضعیت فعلی کشورم را تحلیل کن');if(q)askJessi(q)});syncUI();