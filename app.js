const map=document.getElementById('map');const log=document.getElementById('log');let zoom=1;let day=1;let money=100;function report(text){log.insertAdjacentHTML('beforeend',`<p>${text}</p>`);log.scrollTop=log.scrollHeight}document.querySelectorAll('.city').forEach(btn=>btn.addEventListener('click',()=>report(`شهر ${btn.dataset.city} انتخاب شد.`)));document.getElementById('zoomIn').onclick=()=>{zoom=Math.min(1.8,zoom+.15);map.style.transform=`scale(${zoom})`};document.getElementById('zoomOut').onclick=()=>{zoom=Math.max(.75,zoom-.15);map.style.transform=`scale(${zoom})`};document.getElementById('resetZoom').onclick=()=>{zoom=1;map.style.transform='scale(1)'};document.getElementById('endTurn').onclick=()=>{day++;money+=5;document.getElementById('money').textContent=`${money}M`;report(`روز ${day}: درآمد کشور دریافت شد. نوبت جدید آغاز شد.`)};document.querySelector('.jessi').onclick=()=>report('JESSI: نسخه پایه فعال است. اتصال به داده‌های بازی در مراحل بعدی اضافه می‌شود.');
